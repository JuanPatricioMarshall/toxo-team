extends toxo-layout

block content
    h1= title

    ul
    each player in player_list
        li
            a(href=player.url) #{player.name}
            | (#{player.date_of_birth} - #{player.date_of_death})

    else
        li There are no players.